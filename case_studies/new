--Interface

output;

--Specification

AG((a && b) || (!a && !b));

OG((a && b) || (!a && !b));

/*
AG(((b1 && b2) || (b1 && b3) || (b2 && b3)) -> output);

AG(((!b1 && !b2) || (!b1 && !b3) || (!b2 && !b3)) -> !output);

OG((b1 && b2 && b3) || (!b1 && !b2 && !b3));

*/

